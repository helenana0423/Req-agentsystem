# 需求管理看板（ReqBoard）PRD v1.4

| 项目 | 内容 |
| --- | --- |
| 产品代号 | ReqBoard（暂定） |
| 版本 | v1.4 |
| 文档日期 | 2026-04-24 |
| 作者 | 产品：用户 / 协作：AI Copilot |
| 目标读者 | 产品自查、开发实现、小范围试用团队 |
| 状态 | 待评审 |

---

## 0. TL;DR（一句话说清）

给"没精力维护 TAPD、又被散落在企微/腾讯会议里的需求折磨"的业务侧产品经理，一个**本地可部署的轻量需求看板**，核心特色是：**粘贴一段会议纪要或群聊片段 → Agent 自动抽取需求变更 → 用户确认 → 一键入库**，并支持自然语言问答需求状态与迭代历史。

---

## 1. 背景与问题定义

### 1.1 现状痛点

1. **需求管理工具重、协作工具散**：TAPD 功能全但操作笨重，企微文档靠人肉维护无状态流转，需求进展散落在 N 个群聊和会议纪要里。
2. **多业务并行、跟进人多**：同一个 PM 可能同时对接 3~5 个业务线，每条线有不同负责人和进度节奏，人肉 Tracking 成本极高。
3. **信息沉淀成本高**：一条需求从"提出"到"上线"之间的讨论、边界调整、优先级变更，往往只在某次会议或某个群里讨论过一次，事后难以追溯。
4. **录入门槛高**：要把一次 30 分钟会议沉淀成 TAPD 上结构化的字段更新，PM 通常要花 10~20 分钟手动操作，导致"宁愿不录"。

### 1.2 目标用户画像

| 维度 | 描述 |
| --- | --- |
| 角色 | 业务侧产品经理 / Tech Lead / 项目 PM |
| 团队规模 | 个人或 3~10 人小团队 |
| 现有工具栈 | 企微 + 腾讯会议 + TAPD（可选）+ 各种文档 |
| 典型任务 | 每周跟进 10~30 条需求，分布在 2~5 个业务线 |
| 技术环境 | 有本地部署能力，能跑 Node/Python 服务 |

### 1.3 产品定位

**混合模式（C）**：自己维护一份轻量但完整的需求库作为"真源之一"，TAPD 作为**可选的双向同步目标**（v1.1 再接），企微只做**出站通知**，腾讯会议纪要作为 v1.2 可选的**数据入站源**。

一句话：**"我是 PM 的工作台，不是替代 TAPD 的项目管理系统。"**

**产品形态（v1.4 更新）**：
- **v0.0 纯前端演示版（已交付）**：原生 JS + Tailwind CDN + localStorage，单目录部署、零后端，用作产品评审和内部 demo
- **v1.0 MVP（建设中）**：同一套前端 UI，数据真源切换为后端 FastAPI + SQLite，LLM 真实接入（OpenAI/DeepSeek/Claude），本地单机部署
- **前端样式与交互在 v0.0 已全量确定**，v1.0 以后的迭代只改数据层和后端能力，UI 保持稳定

### 1.4 非目标（明确不做的事）

- ❌ 不做 Bug 跟踪（这是 TAPD/Jira 的主场，重叠没价值）
- ❌ 不做资源排期与人效管理（工时统计、人员负载、成本核算不在范围）
- ✅ 做轻量甘特图（时间线 + 依赖关系 + 延期传导），但不做资源分配类重功能（见 §4.6）
- ❌ 不做多租户 SaaS 版本（MVP 聚焦本地部署 + SQLite）
- ❌ 不自动监听企微群聊（合规、成本、体验三重不合适，详见 §7.2）
- ❌ Agent 不会"自动改库"（所有变更必须人工 confirm，详见 §4.2）

---

## 2. 产品架构一览

```
┌───────────────────────────────────────────────────────────────┐
│                 前端 SPA（React + shadcn/ui）                  │
│   看板视图 / 表格视图 / 详情抽屉 / Agent 面板 / 导入导出       │
└──────────────────────────┬────────────────────────────────────┘
                           │  HTTP / SSE（流式回答）
┌──────────────────────────▼────────────────────────────────────┐
│                后端 API（Python · FastAPI）                    │
│   需求 CRUD / 变更历史 / 导入导出 / Agent 编排 / 文件上传      │
└──┬───────────────┬──────────────────┬───────────────────┬─────┘
   │               │                  │                   │
┌──▼─────────┐  ┌──▼──────────┐  ┌────▼──────────┐  ┌─────▼─────┐
│ 结构化存储 │  │ 知识索引层   │  │  LLM 编排层   │  │ 外部 MCP   │
│ SQLite     │  │ LlamaIndex  │  │ 自研薄封装    │  │ 网关（v1.1）│
│ · 需求     │  │ · 需求索引  │  │ · 抽取器      │  │ TAPD       │
│ · 变更流水 │  │ · 纪要索引  │  │ · 问答器      │  │ 企微机器人 │
│ · 进展动态 │  │ · 文档索引  │  │ · Router      │  │ 腾讯会议   │
│ · 阻塞项   │  │ · 向量库    │  │ function tool │  │            │
│ · 附件     │  │             │  │               │  │            │
│ · 配置     │  │             │  │               │  │            │
└────────────┘  └─────────────┘  └───────────────┘  └────────────┘
                       │                  │
                       └────┬─────────────┘
                            ▼
                 云端 LLM（OpenAI / DeepSeek / Claude，可切换）
```

- **数据层**：SQLite 单文件（结构化）+ 向量库（语义检索），本地持久化，方便备份迁移
- **知识索引层**：LlamaIndex 统一管理多源索引，MVP 只开"需求索引"，后续逐步开"纪要/文档/跨项目"索引
- **LLM 编排层**：**不引入 LangChain**，用轻量自研封装（直接调云端 SDK + function calling），确保 prompt 控制力和可调试性
- **外部 MCP 网关**：v1.0 不接，v1.1 起作为可选扩展

> 详细的技术选型理由、演进路径见 §11。

---

## 3. 数据模型

### 3.1 核心实体

#### 3.1.1 `requirement`（需求主表）

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | uuid | ✓ | 主键 |
| `code` | string | ✓ | 人读编号，如 `REQ-2026-0042`，自动生成 |
| `title` | string | ✓ | 需求标题，≤ 80 字 |
| `business_line` | enum | ✓ | 业务线，枚举值可配置 |
| `owner` | string | ✓ | 负责人（产品/开发双角色用两个字段） |
| `dev_owner` | string |  | 开发负责人 |
| `status` | enum | ✓ | 见 §3.2 状态机 |
| `priority` | enum | ✓ | `P0` / `P1` / `P2` / `P3` |
| `description` | markdown |  | 需求描述，支持富文本 |
| `scope` | markdown |  | 当前功能边界（会被 Agent 问答引用） |
| `source` | string |  | 需求来源，如"2026-04-20 XX 会议" |
| `tags` | string[] |  | 自由标签 |
| `planned_start` | date |  | 计划开始时间（甘特图用） |
| `planned_end` | date |  | 计划完成时间（甘特图用） |
| `expected_release` | date |  | 期望上线时间（= planned_end 的默认值，但可单独覆盖） |
| `actual_start` | date |  | 实际开始时间 |
| `actual_release` | date |  | 实际上线时间 |
| `progress` | int |  | 进度 0-100，可手填或由状态自动映射（详见 §4.6） |
| `estimated_days` | float |  | 预估工作量（人天），可选 |
| `created_at` | datetime | ✓ | 创建时间 |
| `updated_at` | datetime | ✓ | 最近更新时间 |
| `tapd_story_id` | string |  | v1.1 TAPD 同步用 |

#### 3.1.2 `requirement_change`（变更流水，关键）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | uuid | 主键 |
| `requirement_id` | uuid | 外键 |
| `change_type` | enum | `create` / `update` / `status_change` / `scope_adjust` / `comment` |
| `field` | string | 变更字段（update 时必填） |
| `old_value` | text | 旧值 |
| `new_value` | text | 新值 |
| `reason` | text | 变更原因（Agent 抽取时会填"来自 XX 纪要"） |
| `source` | enum | `manual` / `agent_extract` / `import` / `tapd_sync` |
| `source_ref` | text | 原始来源引用（例如会议纪要 ID / 文本片段） |
| `operator` | string | 操作人 |
| `created_at` | datetime |  |

> 💡 这是产品的"护城河"之一——每一次改动都有溯源，Agent 问答"这个需求为什么改了边界"时，能直接回答。

#### 3.1.3 `attachment`（附件）

简单表：`id / requirement_id / filename / filepath / uploaded_by / uploaded_at`。MVP 只存本地路径。

#### 3.1.4 `embedding_chunk`（向量索引，Agent 问答用）

- 每条需求的 `title + description + scope + 最近 10 条 change.reason` 做 chunk 后入向量库
- MVP 用 sqlite-vss 或直接内存里的 FAISS，避免引入额外依赖

#### 3.1.5 `requirement_dependency`（依赖关系，甘特图 + 延期传导用）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | uuid | 主键 |
| `from_id` | uuid | 前置需求 |
| `to_id` | uuid | 后置需求（依赖 from_id 完成） |
| `dep_type` | enum | `FS`（前完后开，默认） / `SS`（同开） / `FF`（同完） / `SF`（罕用） |
| `lag_days` | int | 延后天数，可负（提前量） |
| `note` | text | 备注，例如"需等接口联调完成" |
| `created_by` | string |  |
| `created_at` | datetime |  |

**约束**：
- `(from_id, to_id)` 唯一
- 禁止自依赖（`from_id != to_id`）
- 写入时必须做**环检测**（DFS），若成环则拒绝并返回环上节点列表
- 删除需求时级联删除相关依赖

#### 3.1.6 `progress_note`（进展动态，需求时间线）

每条需求都有一条时间线，记录"今天发生了什么"。这是 ReqBoard 区别于 TAPD 的核心——**进展不是状态枚举值，而是叙事型时间线**。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | uuid | 主键 |
| `requirement_id` | uuid | 外键 |
| `note_type` | enum | `milestone`(里程碑) / `progress`(日常进展) / `blocker`(阻塞) / `unblock`(解除阻塞) / `decision`(决议) / `risk`(风险) / `comment`(评论) |
| `content` | text | 内容，自由文本 |
| `author` | string | 记录人 |
| `source` | enum | `manual` / `agent_extract` |
| `source_ref` | text | 原始来源（Agent 抽取时的原文片段） |
| `linked_requirement_ids` | uuid[] | 关联的其他需求（例如"等 REQ-58 完成"） |
| `linked_users` | string[] | 关联的人员（例如"@张三 在看"） |
| `created_at` | datetime |  |

**设计要点**：
- 进展动态按 `created_at` 倒序展示，形成需求的时间线
- Agent 抽取时自动识别进展/阻塞/决议等句式，生成对应类型的 Note
- 进展动态同时进入向量库，支撑 Agent 的"进展类"问答

#### 3.1.7 `blocker`（阻塞项，结构化卡点）

Blocker 是有状态、未解决就持续亮着警示的强对象。与 Progress Note 的区别：Note 是"发生了什么"（叙事），Blocker 是"当前卡在哪"（结构化待解决项）。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | uuid | 主键 |
| `requirement_id` | uuid | 外键（被阻塞的需求） |
| `blocker_type` | enum | `external_dep`(外部依赖) / `technical`(技术问题) / `cross_team`(跨团队协调) / `pending_decision`(等待决策) / `upstream_req`(前置需求未完成) / `other`(其他) |
| `description` | text | 阻塞描述 |
| `blocking_person` | string | 阻塞方/责任人（例如"法务 @张三"） |
| `blocking_team` | string | 阻塞方团队（可选） |
| `linked_requirement_id` | uuid | 若是等前置需求，指向它（与 `requirement_dependency` 联动） |
| `expected_resolve_date` | date | 预期解除时间（可选） |
| `status` | enum | `active`(活跃) / `resolved`(已解除) |
| `created_at` | datetime |  |
| `resolved_at` | datetime | 解除时间 |
| `resolved_note` | text | 解除说明 |

**约束**：
- 同一需求可有多条 active blocker
- `linked_requirement_id` 非空时，该需求完成（状态→已上线）时自动解除此 blocker 并写入 `unblock` 类型的 Progress Note
- 删除需求时级联删除相关 blocker

**与 `requirement_dependency` 的联动**（详见 §4.7.3）：
- 建立 FS 依赖且前置需求未完成 + 后置需求 `planned_start` 已到 → 自动产生 `upstream_req` 类型的 Blocker

### 3.2 需求状态机

```
   ┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐
   │ 待评审│───▶│ 已立项│───▶│ 开发中│───▶│ 测试中│───▶│ 已上线│
   └──┬───┘    └──┬───┘    └──┬───┘    └──┬───┘    └──────┘
      │           │           │           │
      └───────────┴───────────┴───────────┴─────▶ 已搁置 / 已取消
```

- 默认 6 态，可在配置中裁剪
- 任何状态可以回退到任何状态（不做硬约束，但会在变更流水里记录）

---

## 4. 核心功能（MVP = v1.0）

### 4.1 需求看板（F1）

**入口**：首页默认视图（顶栏正中央的视图切换器切换看板/表格/甘特）

#### 4.1.1 顶栏（全局）

```
[Logo · ReqBoard]  [🔍 搜索框]  [📋 看板 | 🗂️ 表格 | 📊 甘特]  [✨ 粘贴纪要]  [+ 新建需求]  [⋯ 更多]  [👤 用户头像]
```

- **搜索框**：实时筛选（防抖 200ms），按 title/code/description/owner/tags 做模糊匹配
- **视图切换**：三选一 pill 按钮组，高亮当前视图
- **粘贴纪要按钮**：打开 §4.2 Extractor 弹窗
- **新建需求按钮**：打开新建表单模态框（详见 §4.5）
- **更多菜单**：导入 JSON / 导出 JSON / 导出 CSV / 设置 / 重置 Mock 数据
- **用户头像**：当前用户名首字 + 角色说明，点击可进入设置

#### 4.1.2 左侧筛选栏

- **筛选总览**：显示"N/M 条"，有筛选条件时显示"清除"按钮
- **快捷入口**：📋 全部需求 / 🚨 阻塞项（带活跃 blocker 数量徽章）
- **多选筛选组**：业务线 / 负责人 / 状态（带各状态计数）/ 优先级 / 标签
  - 负责人列表来自 requirement.owner 的去重集合
  - 标签列表来自 requirement.tags 的去重集合
  - 列表超过 5 项时出现滚动条
- **使用小贴士**：根据当前视图（kanban/table/gantt）显示不同提示文案

#### 4.1.3 Kanban 视图

- **6 列按状态排布**（待评审/已立项/开发中/测试中/已上线/已搁置），每列有主题色和计数徽章
- **原生 HTML5 拖拽**：跨列拖拽改状态，触发 `requirement_change` 且 Toast 提示 `{code} → {新状态}`
- **卡片元素**（自上而下）：
  - 顶部：优先级徽章（P0/P1/P2/P3 配色）+ 需求编号 + **🚨 阻塞天数徽章**（若有活跃 blocker）
  - 标题（两行截断）
  - 元信息行：业务线小圆点 + 负责人 + 依赖数 🔗N（仅当 > 0 时展示）
  - **进度条 + 百分比 + 截止日期**（"已搁置"状态不显示进度条）
  - **卡片底部信息优先级规则**（关键）：
    - 有活跃 blocker → 显示红底 blocker 描述（单行截断）
    - 无 blocker 但有进展 note → 显示灰底最新一条进展
    - 都没有 → 显示相对时间"⏱ 更新于 X 天前"

#### 4.1.4 Table 视图

- **10 列**：编号 / 标题+标签 / 业务线 / 负责人 / 状态 / 优先级 / 进度条 / 截止日期 / 阻塞 / 更新时间
- **点击列头排序**：默认 `updated_at` 倒序，优先级按 P0 > P1 > P2 > P3 语义排序
- **每行可点击 → 打开详情抽屉**

#### 4.1.5 详情抽屉（Drawer，720px 宽）

**头部**（固定）：
- 需求编号 + 优先级 chip + 状态 chip + 业务线 chip
- 标题 + 👤产品负责人 / 🛠开发负责人 / 更新相对时间
- 右上角两个按钮：**💬 问它一下**（带上下文跳入 Chat，详见 §4.3.3）+ 关闭

**四个 Tab**：

1. **📝 进展时间线**（默认 tab）
   - **活跃阻塞区（置顶）**：每个 blocker 单独卡片，显示类型 chip + "已 N 天" + 预期解除日期 + 描述 + 阻塞方 + 🟢 一键解除按钮（可填解除说明）
   - **Note 时间线**：dot+line 样式（按 note_type 着色），每条含类型 chip + 作者 + 时间 + `agent_extract` 徽章 + 内容 + 可展开的原文片段 details + 关联人（@xxx）
   - **底部快速录入栏**：
     - Note 类型下拉（📝进展 / 🎯里程碑 / ⚖️决议 / ⚠️风险 / 💬评论）
     - 输入框（Enter 发送）
     - 🚨 阻塞按钮 → 打开"新增阻塞"模态框（类型 + 描述 + 阻塞方 + 预期解除日期）

2. **📄 详情**
   - **基本信息区**：业务线 / 状态 / 优先级 / 进度 / 双负责人 / 计划时间 **全字段支持内联编辑**（select/input/date），change 立即写变更流水
   - **描述**：灰底卡片，支持 markdown（粗体 / 列表 / 换行）
   - **Scope（当前功能边界）**：黄底卡片，同样支持 markdown
   - **标签**：紫色 chip 列表
   - **依赖关系**：
     - 前置（upstream）：每条显示 dep_type chip + 前置需求编号/标题/状态
     - 后置（downstream）：同上
   - **🔮 延期传导模拟器**：橙底区块，选个新的完成日期 → 点"模拟影响" → 展示受影响的下游需求清单（code/title/old→new/+N天）

3. **🔄 变更历史**
   - 按时间倒序列表，每条：变更类型 emoji + 字段 chip + **来源徽章**（手动/Agent抽取/导入/TAPD）+ 时间 + 操作人
   - **字段变更 diff**：old 红底删除线 + 绿底新值
   - 变更原因 + 可展开原文片段 details（Agent 抽取时）

4. **📎 附件**
   - MVP 不开放，仅留数据结构；UI 显示"附件功能 v0.5 阶段实现"

**AC（验收标准）**：
- [ ] 100 条需求下，筛选 + 切换视图响应 < 300ms
- [ ] 拖拽变更状态后，变更流水表必须有对应记录，且 Toast 提示
- [ ] 详情 tab 内联编辑生效后立即落库，可在变更历史 tab 看到对应记录
- [ ] 变更历史按时间倒序，diff 高亮准确，来源徽章正确
- [ ] 活跃 blocker 一键解除后自动写入 `unblock` 类型 Progress Note
- [ ] Drawer 展开/关闭有平滑动画，ESC 关闭

### 4.2 Agent 输入：粘贴纪要自动生成变更（F2，核心差异化）

**入口**：顶部 "✨ 粘贴纪要" 按钮

**交互流程**：

```
用户粘贴一段文本（会议纪要 / 群聊片段）
    │
    ▼
Agent 抽取 → 产出"拟执行清单"
    ├── 新增需求 × N（展示标题、负责人、业务线、初始状态）
    ├── 更新需求 × M（展示：哪条需求、改哪个字段、新旧值对比）
    ├── 新增进展 × P（展示：哪条需求、进展类型、内容摘要）
    ├── 新增/解除阻塞 × B（展示：哪条需求、阻塞类型、描述）
    └── 未识别片段（"这段没抽到可执行动作，原文保留"）
    │
    ▼
用户逐项勾选 / 修改 / 忽略
    │
    ▼
点击 "确认写入" → 批量写 requirement & requirement_change
                 （source='agent_extract'，source_ref 记原文片段）
```

**设计要点**：

1. **不做自动写库**。Agent 只产出"提案"，所有操作必须二次确认。理由见 §1.4。
2. **模糊匹配**：更新现有需求时，Agent 用"标题+业务线"做相似度匹配，阈值以下才提示"是否新建"。
3. **来源可追**：每条 change 记录原始文本片段（≤ 500 字），鼠标悬浮可看原文。
4. **批量效率**：一次粘贴最多处理 20 条动作；超过则提示用户拆分。
5. **失败降级**：LLM 抽取失败时，降级为"只帮你创建一条 draft 需求，把原文贴进 description"。
6. **进展/阻塞识别**（v1.3 新增）：抽取器除了 create/update，还识别进展动态和阻塞项（见下方句式表）。

**进展/阻塞识别句式**（ExtractorAgent v2）：

| 原文示例 | 抽取动作 |
| --- | --- |
| "REQ-42 接口联调完成" | → 新增 `progress` Note（type=milestone） |
| "REQ-42 前端 UI 完成 80%" | → 新增 `progress` Note（type=progress） |
| "REQ-42 卡在法务，张三在看" | → 创建 Blocker（type=external_dep）+ `blocker` Note |
| "REQ-42 法务那边回了，可以继续" | → 解除对应 Blocker + `unblock` Note |
| "REQ-42 需要等 REQ-58 做完" | → 建立 REQ-58→REQ-42 的依赖 + 产生 `upstream_req` Blocker |
| "REQ-42 去掉海外白名单功能" | → 新增 `decision` Note + 更新 scope 字段 |
| "REQ-42 有风险，可能影响合规" | → 新增 `risk` Note |

**v1.4 UI 细化（左右两栏布局）**：

```
┌── Extractor 模态框（max-w-5xl）──────────────────────────┐
│ ✨ 粘贴纪要 · Agent 自动抽取      [×]                     │
├─────────────────────────┬────────────────────────────────┤
│ 📋 粘贴会议纪要/群聊片段  │ 🧠 Agent 拟执行清单 (N/M 已选) │
│ [示例1] [示例2]          │ [全选][全不选]                 │
│                          │                                │
│ ┌──────────────────┐    │ ┌────────────────────────┐   │
│ │                  │    │ │☑ 🆕新增需求 · 85%      │   │
│ │  textarea        │    │ │  海外合规改造          │   │
│ │  输入原始纪要     │    │ │  [▶ 原文片段]          │   │
│ │                  │    │ ├────────────────────────┤   │
│ └──────────────────┘    │ │☑ 🔄状态变更 · 80%      │   │
│                          │ │  REQ-42 · 开发中→测试中│   │
│ [🚀 开始抽取]            │ │  [▶ 原文片段]          │   │
│                          │ └────────────────────────┘   │
│                          │ 即将执行 N 个动作 [✓ 确认写入]│
└──────────────────────────┴────────────────────────────────┘
```

**10 种动作类型**（前端 UI 渲染，后端 Prompt 也支持）：

| 类型 | icon | 标签 | 场景 |
| --- | --- | --- | --- |
| create | 🆕 | 新增需求 | 纪要中出现新需求描述 |
| update | ✏️ | 更新需求 | 更新非状态/边界的字段（如 progress、owner） |
| status_change | 🔄 | 状态变更 | 提测/上线/搁置等 |
| scope_adjust | 🎯 | 边界调整 | 去掉/移除功能、移到下个版本 |
| add_progress | 📝 | 新增进展 | 日常推进描述 |
| add_milestone | 🎯 | 里程碑 | 联调完成、评审通过、一阶段完成 |
| add_decision | ⚖️ | 决议 | 明确的决议结论 |
| add_risk | ⚠️ | 风险 | 风险提示（"若...可能影响..."） |
| add_blocker | 🚨 | 新增阻塞 | 卡在XX、等XX确认 |
| resolve_blocker | ✅ | 解除阻塞 | XX 回了、可以继续 |

每条提案卡片包含：
- 动作类型徽章 + 需求编号 + **置信度 %**
- 摘要（一句话说清）
- **字段 diff 对比**（旧值删除线 + 新值绿底，适用于 status_change / scope_adjust / update）
- **原文片段**（details 标签可展开）
- checkbox 勾选/取消

**Agent Prompt 设计思路**（详细 prompt 见附录 A）：

- 输入：原始文本 + 当前系统里该用户可见的需求列表（title + code + business_line + status + scope）+ 活跃 blocker 列表
- 输出：严格 JSON schema，10 种 action 类型

**AC**：

- [ ] 一段典型的 300~800 字会议纪要，抽取耗时 < 15s
- [ ] 抽取结果展示页，每个动作都能单独勾选/取消
- [ ] 示例按钮一键填入，便于演示
- [ ] 确认写入后，所有受影响需求的变更历史里能看到来源为"Agent 抽取"
- [ ] 字段 diff 对比准确（旧值/新值）
- [ ] 抽取失败时，页面明确提示且原文不丢失（降级为"空清单 + 原文保留"）
- [ ] LLM 未启用时，前端本地规则引擎自动兜底（demo 模式可用）

### 4.3 Agent 问答（F3）

**入口**：
- 主入口：右下角固定"💬 问问 Agent"浮条按钮 → 点击展开 420×600 的对话面板
- 次入口：Drawer 头部"💬 问它一下"按钮（带上下文自动提问）

#### 4.3.1 Chat 对话面板

```
┌── AI 需求助手 ────────── [×] ┐
│ 🤖 ReqBoard Agent              │
│ 基于本地需求库回答              │
├────────────────────────────────┤
│ 🤖 你好，我是 ReqBoard Agent    │
│    你可以问我：                 │
│    • 某条需求的进展/阻塞/变更   │
│    • 当前被阻塞的所有需求       │
│    • 某条需求延期 N 天的影响    │
│    • 关键路径/人员维度的汇总    │
│                                │
│ [👤 REQ-42 怎么样？]           │
│ 🤖 **REQ-2026-0042**           │
│    状态：开发中 · 进度 60%      │
│    🚨 活跃阻塞 1：等法务...     │
│    📝 最近进展：...             │
│    📎 引用：REQ-42 时间线       │
├────────────────────────────────┤
│ [REQ-42 现状] [P0 有哪些?] ... │
├────────────────────────────────┤
│ [问一下需求状态...]  [发送] →  │
└────────────────────────────────┘
```

#### 4.3.2 预设问题（快捷入口）

顶部横滚预设按钮（点击即发送）：
- "REQ-42 现在进展怎么样？"
- "哪些需求被阻塞了？"
- "关键路径上有哪些需求？"
- "本周 P0 需求状态"
- "{当前用户}负责的未上线需求"
- "REQ-42 延期 3 天影响哪些下游？"

#### 4.3.3 从 Drawer 联动进 Chat

点击详情抽屉头部"💬 问它一下"按钮：
- 自动展开 Chat 面板
- 自动发送预填问题：`{需求编号} 现在进展怎么样？`
- 用户可继续追问

#### 4.3.4 支持的问法

| 类型 | 示例问题 |
| --- | --- |
| 单条需求进展 | "REQ-2026-0042 现在进展怎么样？" / "XX 功能做到哪了？" |
| **当前阻塞** | "REQ-42 卡在哪了？" / "哪些需求被阻塞了？" / "卡在'外部依赖'的需求有哪些？" |
| **进展时间线** | "REQ-42 最近一周发生了什么？" / "这个需求是怎么一步步推进的？" |
| 变更历史溯源 | "这个需求的功能边界是怎么一步步调整的？" |
| 当前功能边界 | "XX 需求现在的 scope 是什么？" |
| 横向汇总 | "最近一周新增了哪些 P0 需求？" / "XX 业务线未上线的需求有哪些？" |
| 人/时间维度 | "我负责的、本周要上的需求" / "张三手上开发中的需求" |
| **延期影响（依赖 §4.6）** | "REQ-42 如果晚 3 天，会影响哪些下游需求？" / "这周有哪些需求因为前置没做完被卡住？" / "关键路径上有哪些需求？" |

#### 4.3.5 实现

- 单条/横向汇总 → **结构化查询优先**（LLM → SQL / ORM 查询 → 结果）
- 变更历史 / 功能边界 → **RAG**（检索该需求的 change 流水 + description 向量片段 → LLM 总结）
- 延期影响/关键路径 → **调后端工具接口**（propagate_delay / critical_path）
- **回答格式**：Markdown 支持加粗 / 列表 / 换行
- **回答必须带 citations**：底部小字显示"📎 引用：XX · YY"（至少 1 条，帮助用户确认数据来源）
- **流式返回**：SSE 逐 token 渲染，降低首字等待
- **LLM 未启用时降级**：使用前端本地规则引擎回答（意图识别 + 结构化查询，可覆盖 7 类问题）

**AC**：

- [ ] 结构化类问题 p95 响应 < 3s
- [ ] RAG 类问题 p95 响应 < 8s
- [ ] **每个回答底部必须带至少 1 条 citation**
- [ ] 预设问题点击即发送
- [ ] 从 Drawer "💬 问它一下"跳入时自动提问当前需求
- [ ] LLM 未启用时前端规则引擎可覆盖 7 类高频问题

### 4.4 导入导出（F4）

**v1.4 调整：MVP 优先做 JSON，xlsx/csv 降级到 v0.5。**

理由：JSON 格式完整覆盖所有数据（requirements + dependencies + blockers + progressNotes + changes），可用于数据备份/恢复 + 跨机器迁移，开发成本低、用户演示价值高；xlsx/csv 只适合单表导出且丢失关联关系，列在 v0.5 迭代。

#### 4.4.1 JSON 导入导出（MVP）

**导出 JSON**（更多菜单 → 导出 JSON）：
- 导出内容：全量 requirements + dependencies + blockers + progressNotes + changes + exportedAt
- 文件名：`reqboard-export-{YYYYMMDD}.json`
- 用途：备份、跨设备迁移、提交给 PM 评审

**导入 JSON**（更多菜单 → 导入 JSON）：
- 支持拖拽 / 点选上传 `.json` 文件
- **导入策略**：整体替换（覆盖当前所有数据）
- 导入成功后 Toast：`导入成功，共 N 条需求`
- 格式错误 Toast：`导入失败：JSON 格式错误`

#### 4.4.2 CSV 导出（MVP 轻量版）

- 导出 requirements 主表（不含关联实体）
- 字段：code / title / business_line / owner / dev_owner / status / priority / planned_start / planned_end / progress / tags
- 带 UTF-8 BOM（`\uFEFF`），Excel 打开中文不乱码

#### 4.4.3 Excel 完整导入导出（v0.5）

- 支持 `.xlsx`（双向）
- 必须字段：title / business_line / owner / status / priority
- 冲突策略：按 `code` 匹配，存在则更新（记 change），不存在则新建
- 导入前展示"预览 + 冲突提示"
- 多 sheet 导出：requirements / changes / blockers / dependencies / progress_notes

**AC**：

- [ ] JSON 导出一秒内完成（100 条需求）
- [ ] JSON 导入后刷新页面，所有视图显示正确
- [ ] CSV 导出 Excel 打开中文不乱码
- [ ] v0.5 再补 xlsx 能力

### 4.5 基础功能（F5）

#### 4.5.1 新建需求表单

**入口**：顶栏"+ 新建需求"按钮 → 打开模态框

**表单字段**：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 需求编号 | 只读 | - | 自动生成 `REQ-{YYYY}-{NNNN}`（按当年最大编号 +1） |
| 优先级 | select | 是 | P0/P1/P2/P3（默认 P2） |
| 标题 | text | 是 | ≤ 80 字，提示文案"简明扼要描述需求" |
| 业务线 | select | 是 | 从配置 `businessLines` 取 |
| 状态 | select | 是 | 默认"待评审" |
| 产品负责人 | text | - | 默认填入当前用户 |
| 开发负责人 | text | - | 选填 |
| 计划开始 | date | - | 选填（未填时甘特图不展示） |
| 计划完成 | date | - | 选填 |
| 描述 | textarea | - | 支持 markdown（3 行高度默认） |
| 标签 | text | - | 空格分隔，提示"如：合规 海外 性能" |

**AC**：
- [ ] 标题必填未填时 Toast 提示并阻止提交
- [ ] 提交后 Toast：`已创建 {code}`
- [ ] 创建后自动打开该需求的详情抽屉（选做）

#### 4.5.2 设置页

**入口**：更多菜单 → 设置

**设置项**：

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| 当前用户 | text | "王晓琳" | 影响默认 owner、操作人记录 |
| 进度计算模式 | select | 手动填写 | 手动 / 按状态自动映射（见 §4.6.3） |
| **依赖关系自动产生 Blocker**（§4.7.3 联动A） | checkbox | ☐ 关 | 开后：前置未完成 + 后置已到 planned_start → 自动创建 upstream_req Blocker |
| **Blocker 自动顺延 planned_end**（§4.7.3 联动C） | checkbox | ☐ 关 | 开后：active blocker 每多一天，planned_end 自动 +1 天 |

**提示**：底部灰底小字说明"所有设置保存在本地，刷新页面仍生效；重置 Mock 数据可恢复演示数据"

**AC**：
- [ ] 设置保存后立即触发 render，视图、进度等即时响应
- [ ] 两个联动开关切换后立即生效（开/关的行为符合 §4.7.3 约定）

#### 4.5.3 其他基础能力

- 需求手动 CRUD（增/改/删，详情抽屉内联编辑 + 新建模态框）
- 业务线 / 状态 / 优先级 / blocker_type / note_type 枚举由后端 `/api/config` 提供（前端初始化时拉取）
- 全文搜索：顶栏搜索框对 title + code + description + owner + tags 做模糊匹配（防抖 200ms）
- **Toast 提示系统**：4 种样式（success / error / info / warn），定位右上角，2.2 秒自动消失
- **重置 Mock 数据**：更多菜单项，二次确认后清空 localStorage 并重新加载初始 mock（仅 v0.0 前端演示版适用；v1.0+ 通过后端 seed）

### 4.6 甘特图视图（F6，与 Kanban/Table 并列的第三视图）

**入口**：顶部 `[Kanban | Table | Gantt]` 三视图切换

#### 4.6.1 视图组成

```
       4月           5月           6月           7月
       ├─────────────┼─────────────┼─────────────┼────▶
业务线 ▼                                                  │  今天 ┋
─────────────────────────────────────────────────────────┤       ┋
 海外游戏                                                 │       ┋
   REQ-42 [▓▓▓▓▓▓░░░░] 60% ──────▶                      │       ┋
                              └──▶ REQ-43 [▓▓░░░░░░]    │       ┋
                                          └──▶ REQ-45    │       ┋
 工具平台                                                 │       ┋
   REQ-44 [▓▓▓▓▓▓▓▓░░] 80%                              │       ┋
```

关键元素（v1.4 细化）：
- **左侧列表区**（260px 宽）：按业务线分组，**每组顶部有"业务线 · N 个"组头**；组内按 `planned_start` 升序
- **左侧每行**：🚨阻塞标记 + 🔥关键路径标记 + 需求短号 + 标题（截断）+ 优先级 chip
- **主区**：
  - **时间轴 header**（48px 高，sticky）：上层月份（"2026年4月"），下层日/周数字刻度
  - **日/周/月三档缩放**：日档 32px/天 显示每日刻度；周档 14px/天 显示每周一；月档 5px/天 只显进月份分隔
  - **周末列灰底 + 周末数字红色**（日档）
  - **今日红线 + "今天"红标签**，**页面加载时自动滚动到今天**
- **进度条**：按状态色 gradient（待评审灰 / 已立项蓝 / 开发中黄 / 测试中紫 / 已上线绿），条内显示进度 %（> 30% 时白字）
- **依赖连线 SVG**：折线 + 箭头，关键路径上的依赖用红色加粗 + 红箭头
- **Blocker 标记**：有活跃阻塞的需求整行红底高亮，左侧行前加 🚨 emoji（详见 §4.7.3 联动 C）
- **关键路径标记**：节点进度条外加红框（`box-shadow: 0 0 0 2px #dc2626`），左侧行前加 🔥 emoji
- **图例**（顶栏右侧小字）：🟪 进度 · 🟥 关键路径 · 🟥 阻塞中

#### 4.6.2 交互能力（MVP 必做）

| 交互 | 行为 |
| --- | --- |
| 点击进度条 | 弹出详情抽屉（复用 §4.1） |
| 点击左侧行 | 同上 |
| 顶部缩放切换 | 日 / 周 / 月 三档 |
| 关键路径按钮（两模式） | 模式 A：**标亮关键路径**（其他节点仍可见）；模式 B：**仅显示关键路径**（隐藏非关键节点） |
| 筛选 | 复用左侧筛选栏 |
| 加载时自动滚动 | 滚到今天位置（考虑到视口居中） |

**v0.8 追加**：
| 拖拽进度条左右边 | 调整 `planned_start` / `planned_end`，写变更流水 |
| 拖拽进度条整体 | 整段平移 |
| 右键进度条 | "添加依赖" → 进入依赖选择模式，再点一个需求即建立 FS 依赖 |
| 右键依赖箭头 | 删除 / 修改 dep_type / 修改 lag_days |

#### 4.6.3 进度计算规则

`progress` 字段的来源有两种，用户在设置页选择：

- **手动模式**（默认）：用户自己填 0-100
- **状态映射模式**：
  - 待评审 → 0%
  - 已立项 → 10%
  - 开发中 → 50%
  - 测试中 → 80%
  - 已上线 → 100%
  - 已搁置 / 已取消 → 保持原值，灰色显示

#### 4.6.4 延期传导算法（核心能力）

**场景**：用户把 REQ-42 的 `planned_end` 从 5/10 改到 5/15（晚 5 天），系统要回答"下游会被拖到什么时候"。

**算法**（拓扑排序 + BFS）：

```python
def propagate_delay(req_id, new_end_date):
    """返回 {需求id: (原end, 新建议end, 延期天数)}"""
    affected = {}
    visited = set()
    queue = [(req_id, new_end_date)]
    
    while queue:
        cur_id, cur_end = queue.pop(0)
        if cur_id in visited:
            continue
        visited.add(cur_id)
        
        # 查所有 cur_id 作为前置的依赖
        deps = query_deps_where_from(cur_id)
        for dep in deps:
            downstream = get_req(dep.to_id)
            proposed_start = apply_dep_type(cur_end, dep)  # FS/SS/FF/SF + lag
            if proposed_start > downstream.planned_start:
                delay = proposed_start - downstream.planned_start
                new_down_end = downstream.planned_end + delay
                affected[dep.to_id] = (downstream.planned_end, new_down_end, delay.days)
                queue.append((dep.to_id, new_down_end))
    return affected
```

**产品呈现**（v1.4 明确露出位置）：

1. **Drawer 详情 tab 的"🔮 延期传导模拟器"**（MVP 必做）：
   - 选择一个新的完成日期 → 点"模拟影响"
   - dry-run 不写库，只展示受影响的下游清单（code / title / old→new / +N 天）
   - 用户看完可关闭，也可手动编辑 `planned_end` 真正提交
2. **甘特图拖拽进度条**（v0.8 做）：
   - 拖拽时实时计算 → 浮层显示"⚠️ 将影响 N 条下游需求"
   - 松手弹 Modal 让用户选：
     - "批量顺延下游"：每条都产生变更流水（source 标"延期传导自 REQ-42"）
     - "仅改这一条"：只改当前，下游出现**违约提示**（红色警告条）

#### 4.6.5 关键路径算法

- 基于 `planned_start/end` + 依赖关系做 CPM（Critical Path Method）计算
- 输出"如果这条需求延期，整个项目就延期"的路径
- 在甘特图上用高亮色标出

#### 4.6.6 Agent 对接点

F6 不只是一个视图，它让 §4.3 的 Agent 问答解锁三类新问题：

1. **延期影响类**："REQ-42 延期 3 天，影响哪些需求？"
   → Agent 调用 `propagate_delay` 工具，返回结构化结果 + 自然语言解释
2. **关键路径类**："当前卡点在哪里？"
   → Agent 调用 `critical_path` 工具
3. **依赖关系类**："REQ-45 要等哪些需求完成？"
   → Agent 调用依赖查询工具

#### 4.6.7 前端实现

采用 **`@gantt-schedule-timeline-calendar/react`** 或 **`dhtmlx-gantt`（Standard 版免费）** 或自研（基于 `svg` + `dnd-kit`）。选型详见 §11.3。

#### 4.6.8 AC（验收标准）

- [ ] 200 条需求下，甘特图首屏 < 1s
- [ ] 拖拽调整时间 → 实时计算延期传导 < 200ms
- [ ] 依赖关系成环时必须拒绝并提示环上节点
- [ ] 关键路径可一键切换显示
- [ ] 每次时间变更都有变更流水记录
- [ ] Agent 能正确回答"延期 X 天影响哪些下游"类问题

#### 4.6.9 甘特图明确不做

- ❌ 资源分配（谁做什么几天，几个人并发）
- ❌ 工时统计
- ❌ 人员负载可视化
- ❌ 成本核算
- ❌ 多项目全景甘特（先支持单项目维度，跨项目视图放 v2.0）

### 4.7 进展时间线与阻塞管理（F7，核心差异化能力）

> 这是 ReqBoard 与 TAPD 的**本质差异**——TAPD 的"进展"等于一个状态枚举值，ReqBoard 的"进展"等于**叙事型时间线 + 结构化阻塞项 + 自动联动**。

#### 4.7.1 进展时间线视图

**入口**：需求详情抽屉的第一个 tab（默认打开）

**视图**：

```
REQ-42 · 海外合规改造功能
─────────────────────────────────────────────
📌 当前状态：开发中  |  进度 60%  |  ⚠ 1 个阻塞项
─────────────────────────────────────────────
时间线 ▼

04-23 16:00 · 🏁 里程碑
           后端接口联调完成

04-22 10:30 · 🚨 阻塞
           等法务同事 @张三 确认跨境合规
           └ 关联：REQ-58（法务合规评估）
           └ [查看 Blocker #B-017 →]

04-21 14:00 · 📝 进展
           前端 UI 完成 80%，剩表单校验

04-20 09:15 · ✅ 决议
           scope 调整：去掉海外白名单功能

04-18 11:20 · 📌 创建
           需求创建，来源：2026-04-18 产品评审会议纪要
```

**交互（v1.4 细化）**：

1. **活跃阻塞区（置顶 + 红底）**：
   - 每个 blocker 显示：类型 chip + "已 N 天"（红色小字）+ 预期解除日期 + 描述 + 阻塞方
   - 右侧"✓ 解除"按钮，点击后弹 prompt 让用户填解除说明（可空），确认后：
     - blocker.status → resolved
     - 自动写入 `unblock` 类型 Progress Note（"阻塞已解除：XX - {解除说明}"）
     - Toast："阻塞已解除"

2. **Note 时间线（dot+line 样式）**：
   - 每条 note 左侧一个圆点（按 note_type 着色），圆点内显 emoji（🎯/📝/🚨/✅/⚖️/⚠️/💬）
   - 圆点之间用 2px 灰竖线串起来
   - note 主体卡片：类型 chip + 作者 + 时间（带日期和时分）+ Agent 来源徽章（source=agent_extract 时显示）+ 内容（whitespace-pre-wrap）
   - **原文片段**：details 标签可展开，灰底小字，左边缘紫色竖线
   - 关联人：底部小字"@XXX @YYY"

3. **底部快速录入栏**（固定在 Drawer 底部）：
   - Note 类型下拉：📝进展 / 🎯里程碑 / ⚖️决议 / ⚠️风险 / 💬评论（5 选 1，默认进展）
   - 输入框（单行，Enter 直接发送）
   - 发送按钮
   - 🚨 阻塞按钮 → 打开"新增阻塞"模态框：
     - 阻塞类型（6 选 1）
     - 阻塞描述（必填）
     - 阻塞方/责任人
     - 预期解除时间（date 选择器）

#### 4.7.2 Blocker 管理

**Blocker 列表视图**（全局入口：顶部筛选栏加"🚨 阻塞项"筛选器）：

| ID | 所属需求 | 类型 | 描述 | 阻塞方 | 活跃天数 | 预期解除 |
| --- | --- | --- | --- | --- | --- | --- |
| B-017 | REQ-42 | 外部依赖 | 等法务确认跨境合规 | @张三 | 2 天 | 04-25 |
| B-023 | REQ-55 | 技术问题 | 推送服务高延迟排查中 | @李四 | 5 天 | — |

**Blocker 详情**：
- 创建/解除时间线
- 关联的依赖需求（若有）
- 延期影响预览（若已开启甘特图顺延，显示累计顺延天数）

#### 4.7.3 三大联动机制

**联动 A：依赖关系 → 自动产生 Blocker**

触发条件：REQ-42 依赖 REQ-58（FS 关系），且同时满足：
1. REQ-58 状态 ≠ 已上线
2. REQ-42 的 `planned_start` ≤ 今天

→ 系统自动在 REQ-42 上挂一个 `upstream_req` 类型的 Blocker，关联 REQ-58

解除条件：REQ-58 状态变为"已上线" → 自动解除 Blocker + 写入 `unblock` Note

> MVP 阶段此联动默认**关闭**，需在设置页手动开启（避免用户还没建好依赖关系就被一堆自动 Blocker 吓到）。

**联动 B：粘贴纪要 → 自动识别进展/阻塞**

由 ExtractorAgent v2 实现（见 §4.2 设计要点第 6 条）。抽取结果中 `add_progress` / `add_blocker` / `resolve_blocker` 类型的动作，确认后分别写入 `progress_note` / `blocker` 表。

**联动 C：Blocker → 甘特图联动**

1. **视觉警示**：有 active Blocker 的需求，在甘特图上整行**红底高亮**，悬浮显示卡点摘要
2. **自动顺延**（默认关，设置页可开启）：active Blocker 每多存在 1 天，`planned_end` 自动顺延 1 天，下游依赖同步顺推（复用 §4.6.4 延期传导算法）
3. **Agent 问答增强**：
   - "REQ-42 卡了几天了？" → 查 blocker 活跃天数
   - "如果今天不解除，REQ-45 会被推到什么时候？" → blocker 天数 × 顺延 + 延期传导
   - "目前所有被阻塞的需求有哪些？" → 聚合查询 active blocker

#### 4.7.4 需求卡片上的进展展示（Kanban 视图）

```
┌───────────────────────────────┐
│ REQ-42 · 海外合规改造         │
│ 👤 @李四  P1  开发中 60%      │
│ ────────────────────────────  │
│ 🚨 卡在法务（已 2 天）        │  ← Blocker 醒目显示
│ 📝 04-22 · 接口联调完成       │  ← 最新一条进展（1 行）
└───────────────────────────────┘
```

- 有 active blocker 时，卡片左上角显示 🚨 图标 + blocker 活跃天数
- 卡片底部展示最新一条进展 Note（只显示 1 行，点击进详情看完整时间线）
- 无 blocker 且无进展时，显示最近一次变更摘要

#### 4.7.5 AC（验收标准）

- [ ] 进展时间线按时间倒序展示，不同类型有不同图标和颜色
- [ ] Blocker 列表可按类型/活跃天数/业务线筛选
- [ ] Agent 抽取能正确识别进展/阻塞/解除句式（准确率 ≥ 70%）
- [ ] 依赖关系→Blocker 联动可配置开关
- [ ] 甘特图上有 blocker 的需求红底高亮
- [ ] Blocker 解除后自动写入 unblock Note
- [ ] Agent 能回答"卡在哪/卡了多久/哪些需求被阻塞"类问题

---

## 5. 外部系统对接（v1.1+，不在 MVP 范围，但架构要预留）

### 5.1 TAPD 双向同步（v1.1）

**基于**：`mcp-server-tapd`（PyPI，v8.x，2026 年仍在维护）

**鉴权**：TAPD 个人访问令牌（用户在设置页填入即可，无需开放应用 OAuth）

**同步模式**：

- **手动触发**：需求详情页加"推送到 TAPD"按钮
- **定时增量**：可选每 N 分钟拉一次该用户的 TAPD 变更
- **字段映射**：配置页面让用户自己映射（本地 `status` ↔ TAPD `status`）

**已知限制**（基于调研）：

- MCP 涵盖需求/缺陷/任务/迭代的 CRUD，但自定义字段支持需看源码确认
- 不支持前端直连 TAPD API（会泄露密钥），所有调用走后端

### 5.2 企微机器人推送（v1.1）

**基于**：`wecom-bot-mcp-server`（或直接调 webhook，MCP 不是必须）

**用途**：**仅出站**，不做入站

- 每日/每周需求日报推送到指定群
- P0 需求状态变更实时推送
- "@我" 的需求变更提醒（基于配置的 userid 映射）

### 5.3 腾讯会议纪要拉取（v1.2，可选）

**基于**：腾讯会议「智能纪要」REST API

**前置条件**：企业开通商业版及以上，配置 OAuth 应用

**功能**：会议结束后自动拉取纪要 → 推送到 Agent 输入面板，用户决定是否提取

### 5.4 明确不做的对接

- **企微群聊记录读取**：需要「会话内容存档」付费服务 + 管理员配合，合规和成本都不划算，产品不承诺这条路径

---

## 6. 非功能需求

| 维度 | 要求 |
| --- | --- |
| 部署形态 | MVP 本地单机部署（一键启动脚本），前后端同源 |
| 存储 | SQLite 单文件，自动每日备份到 `.workbuddy/backup/` |
| 性能 | 需求规模 1000 条内，任何页面 < 500ms 首屏 |
| 并发 | MVP 单用户，v1.1 起小团队（< 10 人） |
| 隐私 | 所有数据留在本地；LLM 调用走用户自己的 API Key（支持切换 OpenAI / DeepSeek / Claude / 本地模型） |
| 日志 | 所有 Agent 的原始输入/输出落盘，方便复盘 |
| 错误处理 | LLM 失败必须优雅降级，绝不能导致需求丢失 |

---

## 7. 风险与应对

### 7.1 LLM 抽取不准

- **应对**：所有变更走人工确认；抽取失败降级为"draft + 原文保留"
- **度量**：上线后跟踪"抽取后被用户修改/忽略的比例"，目标 < 30%

### 7.2 企微群聊记录的迷思

- **不要自动监听群聊**。原因：
  1. 合规路径（会话内容存档）要管理员审批 + 付费
  2. 全量消息流 ≠ 可直接抽取的会议纪要，噪声极大
  3. 体验上"AI 自动监听我的聊天"心理负担很重
- **替代方案**：引导用户"开会结束后，花 30 秒粘贴纪要"，这是完全可接受的操作

### 7.3 数据分裂（本地库 vs TAPD）

- v1.0 不接 TAPD，问题不存在
- v1.1 接入时，明确"某一条需求的真源字段"：默认本地为真源，TAPD 字段由本地覆盖；用户可逐字段切换

### 7.4 个人使用场景下的数据备份

- 自动每日备份 SQLite 到 `.workbuddy/backup/YYYY-MM-DD.db`
- 保留 30 天

---

## 8. 交互与视觉（要点说明）

### 8.1 布局

```
┌─────────────────────────────────────────────────────────────────────┐
│ [LOGO] ReqBoard  [搜索]  [📋看板|🗂️表格|📊甘特]  [✨粘贴纪要] [+新建] [⋯] [👤] │
├─────────────┬───────────────────────────────────────────────────────┤
│ 筛选侧栏     │  视图内容区                                           │
│ 📋 全部     │                                                       │
│ 🚨 阻塞(3)  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐    │
│             │  │待评审│ │已立项│ │开发中│ │测试中│ │已上线│ │已搁置│   │
│ 业务线 ✓   │  │ 卡片│ │ 卡片│ │ 卡片│ │ 卡片│ │ 卡片│ │ 卡片│    │
│ 负责人     │  │ ... │ │ ... │ │ ... │ │ ... │ │ ... │ │ ... │    │
│ 状态(带计数)│                                                       │
│ 优先级     │  💡 使用小贴士（随视图变化的引导文案）                 │
│ 标签       │                                                       │
│             │                                             💬浮条 → │
└─────────────┴───────────────────────────────────────────────────────┘
```

- **右下角固定"💬 问问 Agent"浮条**，点击展开 420×600 的对话面板（详见 §4.3.1）
- **视图切换在顶栏正中央**（不是主区顶部），筛选条件跨视图共享
- **卡片悬浮显示 tooltip**：标题（因截断时）+ 阻塞描述（有 blocker 时）

### 8.2 关键细节

- **配色**：中性色系 + **紫色品牌色（brand）** 作为主操作色；`P0` 红、`P1` 橙、`P2` 蓝、`P3` 灰
- **图标**：Lucide（通过 `lucide-static` CSS class 引入），偶尔用 emoji 做强情感识别（🚨/🔥/✨）
- **动效**：Drawer 右侧滑入、Modal 淡入、拖拽平滑，其他页面保守克制
- **字体**：优先级 PingFang SC / Microsoft YaHei / system-ui

### 8.3 Toast 通知系统

右上角定位（top: 72px; right: 24px），自动消失时长 2.2s，4 种样式：

| 类型 | 图标 | 左边框色 | 场景 |
| --- | --- | --- | --- |
| success | ✅ | 绿色 | 创建/修改/解除成功 |
| error | ❌ | 红色 | 校验失败、网络错误 |
| info | ℹ️ | 蓝色 | 普通提示 |
| warn | ⚠️ | 橙色 | 风险提示（如延期传导 dry-run 结果） |

Toast 不阻断用户操作，支持多条并存（自上而下堆叠）。

### 8.4 视图小贴士（侧栏底部）

根据当前视图变化的引导文案：

- **Kanban**：拖拽卡片可改变状态；有🚨标记的需求表示存在活跃阻塞项
- **Table**：点击列头可排序；多条件叠加筛选
- **Gantt**：红底行表示存在阻塞；红框进度条为关键路径节点；点击进度条可查看详情

（交互原型 HTML 已在 v0.0 阶段交付，保留在 `.workbuddy/legacy/vanilla-src-uploaded-*/` 下）

---

## 9. 里程碑与排期（个人节奏）

| 版本 | 范围 | 状态 | 预计耗时 |
| --- | --- | --- | --- |
| **v0.0**（纯前端演示版） | 原生 JS + Tailwind CDN + localStorage，全量 UI（Kanban/Table/Gantt/Drawer/Chat/Extractor）+ 规则引擎 mock Agent | ✅ **已交付** | 1 周（一次性） |
| **v0.1**（后端骨架） | FastAPI + SQLModel + 6 张表 + 全量 REST API，前端 store 切换为调后端 | 🚧 建设中 | 2~3 天 |
| **v0.3**（基础看板联调） | F1 + F4 JSON 导入导出 + F5 新建/设置，前后端联调跑通 | 📅 | 2 天 |
| **v0.5**（Agent 真实接入） | F2 LLM 抽取真实调用 + F3 结构化问答 + xlsx/csv 导入导出 | 📅 | 4~6 天 |
| **v0.7**（进展模块后端化） | F7 进展时间线 + Blocker 管理 + 卡片进展展示（存真源在后端） | 📅 | 3~4 天 |
| **v0.8**（甘特初版） | F6 甘特图（时间线 + 依赖 + 拖拽 + Blocker 联动，不含关键路径） | 📅 | 4~5 天 |
| **v1.0 MVP** | + F6 关键路径 + 延期传导真实联动 + Agent 延期/阻塞问答 + F3 RAG 问答 + F2 进展/阻塞抽取 | 📅 | 3~4 天 |
| v1.1 | TAPD MCP 同步 + 企微推送 | — | 3~5 天 |
| v1.2 | 腾讯会议纪要对接 + 纪要归档索引 | — | 3~4 天 |
| v1.3 | 文档库索引 + Chroma 升级 | — | 4~6 天 |

> **与 v1.0 估算对比**：因纳入完整甘特图，MVP 工期从 ~2 周膨胀到 ~3.5~4 周。按每天 2~3 小时有效开发时间估算。

> 风险提示：甘特图的**延期传导 + 关键路径 + 依赖成环检测**三块，是整个项目里最容易返工的部分，建议在 v0.8 做"简单版"、v1.0 补齐"完整版"，分两段打磨。

---

## 10. 度量指标（上线后怎么判断有用）

| 指标 | 目标（个人使用） |
| --- | --- |
| 每周粘贴纪要次数 | ≥ 3 次 |
| Agent 抽取后被采纳的动作占比 | ≥ 70% |
| 每周手动 CRUD 次数 | 相比用 TAPD 时 ↓ 50% |
| Agent 问答次数 | ≥ 5 次/周 |

---

## 附录 A · Agent 抽取 Prompt 草案（v1.4 · 10 种动作）

````
系统：你是 ReqBoard 需求管理助手，负责从会议纪要/群聊片段中抽取需求相关的"动作"。

规则：
1. 只抽取明确达成共识的动作，"讨论中未决议"的不要抽。
2. 输出严格 JSON，字段见 schema。
3. 需求匹配：按编号或标题语义匹配"已有需求列表"；匹配到输出 update/status_change/.../add_blocker，否则输出 create。
4. 每个动作必须带 sourceText（原文引用，≤ 200 字）。
5. 不要编造字段值；不确定留空。
6. 对一句话可以抽出多个动作（例如"REQ-42 卡在法务"同时产生 add_blocker + add_progress）。

输入：
- conversation_text: <用户粘贴的原文>
- existing_requirements: [{code, title, business_line, status, owner, scope}, ...]
- existing_blockers: [{id, requirement_id, blocker_type, description, status}, ...]
- business_lines: [...]   # 可选枚举
- users: [...]            # 可选枚举

Action 类型（10 种）：
- create           新增需求
- update           字段更新（非 status/scope 的字段，如 progress、owner）
- status_change    状态变更（提测/上线/搁置等）
- scope_adjust     边界调整（去掉/移除功能）
- add_progress     普通进展
- add_milestone    里程碑（联调完成、评审通过）
- add_decision     决议
- add_risk         风险提示
- add_blocker      新增阻塞
- resolve_blocker  解除阻塞

Blocker 类型：external_dep / technical / cross_team / pending_decision / upstream_req / other
Note 类型：milestone / progress / blocker / unblock / decision / risk / comment

输出 JSON schema：
{
  "actions": [
    {
      "action": "create" | "update" | "status_change" | "scope_adjust"
              | "add_progress" | "add_milestone" | "add_decision" | "add_risk"
              | "add_blocker" | "resolve_blocker",
      "reqCode": "REQ-2026-0042",      // 非 create 时必填
      "summary": "一句话摘要",
      "payload": {
        // create/update 用
        "title": "...", "business_line": "...", "owner": "...",
        "status": "...", "priority": "...", "scope": "...", "description": "...",
        "progress": 60,

        // add_progress/add_milestone/add_decision/add_risk 用
        "content": "进展/里程碑/决议/风险的内容",

        // add_blocker 用
        "blocker_type": "external_dep",
        "description": "阻塞描述",
        "blocking_person": "法务·张三",
        "linked_requirement_code": "REQ-...",  // 若是等前置需求

        // resolve_blocker 用
        "blocker_description_hint": "匹配哪个 blocker 的描述关键词",
        "resolved_note": "解除说明"
      },
      "diff": {                         // 字段变更时填（可选）
        "field": "status",
        "old": "开发中",
        "new": "测试中"
      },
      "sourceText": "原文片段",
      "confidence": 0.85                // 0~1
    }
  ],
  "unprocessed_segments": ["...", "..."] // 明确没抽出动作的段落
}

只输出 JSON，不要输出其他内容。
````

---

## 附录 B · 字段字典（MVP 默认配置）

#### B.1 基础枚举

- **业务线**（可改）：`PUBG Mobile` / `Honor of Kings` / `彩虹六号` / `Nikke` / `GunStar`（v1.4.1 起调整为全游戏业务线）
- **状态**：`待评审` / `已立项` / `开发中` / `测试中` / `已上线` / `已搁置`
- **优先级**：`P0` / `P1` / `P2` / `P3`
- **需求编号规则**：`REQ-YYYY-NNNN`，NNNN 在当年内自增

#### B.2 进展 Note 类型（7 种）

| 枚举 | 标签 | icon | 色调 | 典型场景 |
| --- | --- | --- | --- | --- |
| `milestone` | 里程碑 | 🎯 | 紫色 | 联调完成、功能完成、评审通过 |
| `progress` | 进展 | 📝 | 蓝色 | 日常推进（"前端 UI 完成 80%"） |
| `blocker` | 阻塞 | 🚨 | 红色 | 新增 blocker 时自动记一条 |
| `unblock` | 解除阻塞 | ✅ | 绿色 | 解除 blocker 时自动记一条 |
| `decision` | 决议 | ⚖️ | 琥珀色 | 明确的决议结论 |
| `risk` | 风险 | ⚠️ | 橙色 | 风险提示 |
| `comment` | 评论 | 💬 | 灰色 | 普通备注、创建动作 |

#### B.3 Blocker 类型（6 种）

| 枚举 | 标签 | 典型场景 |
| --- | --- | --- |
| `external_dep` | 外部依赖 | 等法务/合规/合作方等 |
| `technical` | 技术问题 | 卡技术难点、架构改造 |
| `cross_team` | 跨团队协调 | 需要其他团队配合 |
| `pending_decision` | 等待决策 | 等领导/客户拍板 |
| `upstream_req` | 前置需求未完成 | 由依赖关系联动自动产生（§4.7.3 联动A） |
| `other` | 其他 | 兜底 |

#### B.4 变更流水来源（4 种）

| 枚举 | 标签 | 徽章色 | 场景 |
| --- | --- | --- | --- |
| `manual` | 手动 | 灰色 | 用户直接编辑 |
| `agent_extract` | Agent抽取 | 紫色 | Extractor 粘贴纪要抽取后写入 |
| `import` | 导入 | 蓝色 | JSON/Excel 导入产生 |
| `tapd_sync` | TAPD | 靛蓝 | v1.1 TAPD 双向同步 |

#### B.5 依赖类型（4 种）

| 枚举 | 含义 | MVP UI 暴露 |
| --- | --- | --- |
| `FS` | Finish-to-Start（前完后开，默认） | ✅ 是 |
| `SS` | Start-to-Start（同开） | ⏳ 数据模型已留，UI 后补 |
| `FF` | Finish-to-Finish（同完） | ⏳ 同上 |
| `SF` | Start-to-Finish（罕用） | ⏳ 同上 |

#### B.6 进度计算模式（设置可切换）

- **手动模式**（默认）：用户自己填 0-100
- **状态映射模式**：
  - 待评审 → 0% / 已立项 → 10% / 开发中 → 50% / 测试中 → 80% / 已上线 → 100%
  - 已搁置 / 已取消：保留原值，卡片灰色显示

---

## 附录 C · MCP 对接能力速查

| 目标系统 | MCP/接口 | 能力 | MVP 是否接入 |
| --- | --- | --- | --- |
| TAPD | `mcp-server-tapd` (PyPI) | 需求/缺陷/任务/迭代 CRUD，个人令牌鉴权 | ❌ v1.1 |
| 企微（出站） | `wecom-bot-mcp-server` 或直调 webhook | 群机器人发消息 | ❌ v1.1 |
| 企微（入站群聊） | 会话内容存档 SDK | 需付费 + 管理员 | ❌ 不做 |
| 腾讯会议 | 智能纪要 REST API | 会后纪要拉取 | ❌ v1.2 |

---

## 11. 技术选型与 AI 架构

### 11.1 核心原则

1. **薄 LLM 层，厚 RAG 层**——LLM 调用保持可控、可调试；RAG 用专业工具，避免重复造轮子。
2. **不为未来过度设计，但为未来预留接口**——MVP 不做跨项目知识库，但索引层的抽象要支持"多数据源接入"。
3. **LLM 供应商可切换**——所有模型调用走统一的 `LLMClient` 接口，换一家只改 config。
4. **所有写库操作必须人工确认**（与 §4.2 呼应）。

### 11.2 为什么不用 LangChain

| 维度 | 直接评估 |
| --- | --- |
| 抽象层厚度 | LangChain 的 `Runnable/Chain/LCEL` 对"单轮 LLM + function calling"来说过度抽象 |
| API 稳定性 | v0.0 → v0.1 → v0.2 → v0.3 频繁重构，升级成本高 |
| Prompt 控制力 | PromptTemplate + OutputParser 会削弱对 prompt 的精确控制（附录 A 的严格 JSON schema prompt 需要 100% 控制） |
| 调试体验 | 栈深、错误信息隐晦，排查成本高 |
| 我们的场景 | 只有"单轮抽取"和"RAG 问答 + 结构化查询"两条 pipeline，够不上 LangChain 的复杂度 |

**结论**：LangChain 不是坏东西，但**对 ReqBoard 的 AI 复杂度来说，它解决的问题我们没有**。

> 注：如果未来需要做**多步自主 Agent**（例如"规划一个需求评审流程 → 自主调用多个工具"），我们会考虑 **LangGraph**（LangChain 生态里**状态图**那个子项目），而不是 LangChain 本身。LangGraph 抽象更薄、更适合多步控制流。

### 11.3 最终技术栈

#### 后端

| 组件 | 选型 | 理由 |
| --- | --- | --- |
| Web 框架 | **FastAPI** | Python 里最成熟，异步原生，自动 OpenAPI，SSE 简单 |
| 数据库 | **SQLite**（MVP）→ PostgreSQL（团队协作版） | 零依赖启动，迁移路径清晰 |
| ORM | **SQLModel**（SQLAlchemy + Pydantic） | 和 FastAPI 同作者，类型系统统一 |
| 向量库 | **sqlite-vss**（MVP）→ **Chroma**（知识库扩展期） | MVP 不引入额外服务，扩展期再升级 |
| RAG 框架 | **LlamaIndex** | 专为"数据→索引→检索"而生，多数据源、多索引类型支持完整 |
| LLM 调用 | **OpenAI SDK / Anthropic SDK + 自研 `llm_client.py` 薄封装** | 保持 prompt 控制力，用统一接口抽象多厂商 |
| Function Calling | 各家 SDK 原生 `tools` 参数 | 不引入中间层 |
| Prompt 管理 | 纯 Python 模块 + Jinja2（可选） | prompt 不多，不需要 LangChain Hub |
| 后台任务 | **APScheduler** | 日报推送、定时索引更新 |
| MCP 客户端（v1.1+） | `fastmcp` 或官方 MCP Python SDK | 与 LLM 框架解耦 |

#### 前端（与 v1.0 PRD 一致）

React + Vite + TypeScript + Tailwind + shadcn/ui + dnd-kit + Chart.js

**甘特图组件选型**（F6 专属）：

| 方案 | 优点 | 缺点 | 推荐度 |
| --- | --- | --- | --- |
| `dhtmlx-gantt`（Standard 版 GPL v2 免费） | 成熟、文档全、依赖/关键路径开箱即用 | GPL 协议（商业化要买授权）、样式定制度中等 | ⭐⭐⭐⭐ **MVP 首选** |
| `frappe-gantt` | 轻量、MIT、样式可改 | 无依赖关系内置、要自己画箭头 | ⭐⭐⭐ 备选 |
| `@dnd-kit` + `d3` 自研 | 完全可控、无版权问题 | 工期膨胀 3~5 天 | ⭐⭐ 不推荐 MVP 阶段 |
| `gantt-task-react` | React 原生、依赖支持 | 维护不活跃 | ⭐⭐ 备选 |

**决定**：MVP 先上 `dhtmlx-gantt`（个人使用不涉及 GPL 传染），跑通流程；若后续有商业化诉求再切到 `frappe-gantt` 或自研。

#### 部署

- **MVP**：单机 `docker-compose up`，前端静态文件由 FastAPI 直接 serve
- **后续**：前后端独立容器 + Nginx 反代

### 11.4 AI 架构详解

```
┌──────────────────────────────────────────────────────────────┐
│                    AI 架构（Python 侧）                      │
└──────────────────────────────────────────────────────────────┘

  用户场景                编排组件              底层能力
  ─────────              ────────              ────────

  粘贴纪要   ──▶ ExtractorAgent      ──▶ LLMClient (function calling)
                （严格 JSON 输出）        │
                                          └─▶ Cloud LLM (OpenAI/DeepSeek/Claude)

  需求问答   ──▶ RouterAgent         ──▶ 分流：
                （判断问题类型）          ├─▶ StructuredQA
                                         │    └─▶ Text2SQL → SQLite → LLM 总结
                                         ├─▶ SemanticQA
                                         │    └─▶ LlamaIndex RetrievalEngine
                                         │         ├─▶ 需求索引（MVP）
                                         │         ├─▶ 纪要归档索引（v1.2）
                                         │         └─▶ 文档库索引（v1.3）
                                         └─▶ ScheduleQA (v1.0 新增)
                                              └─▶ 调度工具集
                                                   ├─▶ propagate_delay()
                                                   ├─▶ critical_path()
                                                   ├─▶ query_dependencies()
                                                   ├─▶ list_blockers()
                                                   └─▶ blocker_impact()

  知识中台   ──▶ KnowledgeHub        ──▶ LlamaIndex MultiModalRouter
  （未来）       （跨项目检索）            ├─▶ 项目 A 索引
                                          ├─▶ 项目 B 索引
                                          └─▶ 公共文档索引
```

**关键组件说明**：

- **`LLMClient`**（自研，约 150 行）：封装 OpenAI / DeepSeek / Claude 三家 SDK，统一 `chat()` / `chat_with_tools()` / `embed()` 三个方法，config 里切换
- **`ExtractorAgent`**：执行附录 A 的抽取 prompt，强制 JSON 模式，带失败重试与降级
- **`RouterAgent`**：根据问题语义判断走结构化查询还是语义检索（MVP 可以简化为"带需求编号 → 结构化，否则 → 语义"）
- **`RetrievalEngine`**（基于 LlamaIndex）：
  - 统一的 `Node`/`Document` 抽象
  - 支持按 `source_type`（requirement / meeting / doc）过滤检索
  - 新增数据源只需注册一个 `Reader`

### 11.5 RAG 演进路径（对应你选的 D 选项）

| 阶段 | 索引范围 | 新增能力 | 技术动作 |
| --- | --- | --- | --- |
| **v1.0 MVP** | 需求库自身（title + description + scope + 变更流水） | 需求问答 | sqlite-vss + LlamaIndex 最小化接入 |
| **v1.2** | + 归档的会议纪要（用户粘贴过的原文） | "XX 需求当初是怎么讨论出来的" | 新增 `MeetingNotesReader`，按会议日期/参与人切片 |
| **v1.3** | + 上传文档（PRD、设计稿、业务白皮书） | 文档 QA、跨文档比对 | 新增 `DocumentReader`（支持 docx/pdf/md），引入 Chroma 替换 sqlite-vss |
| **v2.0 知识中台** | + 多项目、多团队、跨库检索 | 企业级知识检索 | 索引层抽象成独立服务；引入权限模型；接 SSO |

**关键设计决策（现在就要定）**：

1. **索引粒度**：每条需求 = 一个 `Document`，内部按 `title` / `description` / `scope` / `change_log` 切成多个 `Node`。好处是检索出来能精确到"这条需求的变更流水"级别。
2. **向量化时机**：写入/修改时**同步**更新索引（不走定时重建），保证实时性。
3. **Embedding 模型**：MVP 用 `text-embedding-3-small`（OpenAI，便宜够用）或 DeepSeek 的 embed 模型。**未来做知识中台时再评估是否需要自部署 bge-m3。**
4. **索引存储位置**：MVP 就在 `.workbuddy/index/` 下，和 SQLite 一起备份。

### 11.6 LLM 供应商策略

- **配置文件驱动**：`config.yaml` 里设 `llm.provider = openai | deepseek | anthropic`，`llm.model = ...`
- **抽取任务 vs 问答任务可以用不同模型**：抽取用强结构化能力的模型（Claude / GPT-4o-mini 性价比高），问答用便宜快速的（DeepSeek-V3 / GPT-4o-mini）
- **API Key 来源**：用户自己的 Key，存本地 `.env`，不上传服务器

### 11.7 成本与性能预期（MVP）

| 场景 | 单次成本（估算） | 延迟 | 备注 |
| --- | --- | --- | --- |
| 抽取一段 500 字纪要 | < ¥0.05 | 5~15s | 用 GPT-4o-mini 或 DeepSeek-V3 |
| 一次需求问答 | < ¥0.02 | 2~8s | 结构化查询几乎零 token |
| Embedding 100 条需求 | < ¥0.1 | < 10s | 批量调用 |
| 月度总成本（个人使用） | < ¥20 | — | 假设每月 100 次抽取 + 500 次问答 |

### 11.8 不做什么（AI 架构层面）

- ❌ 不用 LangChain（理由见 §11.2）
- ❌ 不做本地模型推理（至少 v1.x 内）——用户明确选云端 API
- ❌ 不做自研 RAG 实现（重复造轮子，LlamaIndex 已经很成熟）
- ❌ 不做 Fine-tuning（指令遵循靠好的 prompt 就够）
- ❌ 不做 Agent 的长期记忆持久化（除变更流水外，Agent 每次对话无状态；需要历史信息就查 SQLite）

---

## 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-04-23 | 初稿 |
| v1.1 | 2026-04-23 | 新增 §11 技术选型与 AI 架构；升级 §2 架构图（Python/FastAPI/LlamaIndex/不用 LangChain） |
| v1.2 | 2026-04-23 | 纳入完整甘特图至 MVP：新增 §4.6（含依赖/延期传导/关键路径）、§3.1.5 依赖关系表、§4.3 延期影响类问答、§11.3 甘特图组件选型；§9 里程碑按 3.5~4 周节奏重排 |
| v1.3 | 2026-04-23 | 新增进展/阻塞模块：§3.1.6 progress_note 表、§3.1.7 blocker 表、§4.7 进展时间线与阻塞管理（含三大联动机制）、§4.2 Agent 抽取升级（进展/阻塞/依赖句式识别）、§4.3 问答加阻塞/进展类问题、附录 A Prompt schema 扩展、§9 里程碑加 v0.7 阶段 |
| v1.4 | 2026-04-24 | **对齐 v0.0 纯前端演示版的已有形态**：§1.3 加"产品形态"说明、§4.1 细化顶栏/侧栏/三视图/Drawer 4 tab、§4.2 Extractor 扩展为 10 种动作 + 左右两栏 UI、§4.3 Chat 浮条形态 + 预设问题 + citations + Drawer 联动、§4.4 MVP 优先 JSON 导入导出、§4.5 新建表单 + 设置页字段清单、§4.6 甘特图按业务线分组 + 周末高亮 + 图例 + 关键路径两模式、§4.7 进展时间线 dot+line + 底部快速录入栏 + Blocker 一键解除、§8 顶栏结构 + Toast 系统 + 视图小贴士、§9 加 v0.0 里程碑、附录 A Prompt schema 扩到 10 种动作、附录 B 补充 note_type/blocker_type/source 完整表 |
